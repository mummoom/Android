<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/item_dogprofile_profile_iv"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="20dp"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@drawable/myprofile_img"
        />
    <TextView
        android:id="@+id/item_dogprofile_dogname_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="다몽이"
        android:textColor="@color/item_dogprofile_tv"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/item_dogprofile_profile_iv"
        app:layout_constraintStart_toStartOf="@id/item_dogprofile_profile_iv"
        app:layout_constraintEnd_toEndOf="@id/item_dogprofile_profile_iv"
        android:layout_marginTop="5dp"
        />

</androidx.constraintlayout.widget.ConstraintLayout>